node_modules
dist
site
.lwr
__lwr_cache__
*.log
.DS_Store
.env
.env.local
.vercel

# Note: src/modules/c/resumeData/resumeData.ts is generated from ../main.tex
# but is committed so Vercel CLI deploys (which only upload web/) succeed.
# Run `npm run sync` (or `npm run ship`) to refresh it.
